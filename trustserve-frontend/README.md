# TrustServe Frontend

Premium React frontend for TrustServe marketplace.

## Environment

Create `.env` from `.env.example`:

- `VITE_API_BASE_URL=http://localhost:5000/api`

## Run

- `npm install`
- `npm run dev`

## Build

- `npm run build`
